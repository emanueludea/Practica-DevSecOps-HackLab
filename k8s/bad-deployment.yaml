apiVersion: apps/v1
kind: Deployment
metadata:
  name: bad-deployment
spec:
  selector:
    matchLabels: { app: bad }
  template:
    metadata:
      labels: { app: bad }
    spec:
      containers:
        - name: bad
          image: demo-app:local@sha256:1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
          resources:
            limits:
              cpu: "500m"
          # sin securityContext
